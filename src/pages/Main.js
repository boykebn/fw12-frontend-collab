import { BrowserRouter, Route, Routes } from "react-router-dom";
import React from "react";

const Main = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Route></Route>
      </Routes>
    </BrowserRouter>
  );
};

export default Main